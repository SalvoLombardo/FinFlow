from typing import Any

# Phase 1.5: full implementation


async def apply_recurrences(week_id: str, user_id: str, db: Any) -> None:
    """Copy recurring transactions from the previous week into week_id if not already present."""
    raise NotImplementedError("Implement in Phase 1.5")
